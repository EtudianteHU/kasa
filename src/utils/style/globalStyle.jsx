import { createGlobalStyle } from 'styled-components'

const stylegGlobalStyle = createGlobalStyle`
    * {
        font-family: 'Montserrat', sans-serif;
        margin: 0;
        padding: 0;
    }

`

export default stylegGlobalStyle
